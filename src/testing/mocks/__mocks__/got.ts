export const mockGet = jest.fn(() => ({}));
export const mockPost = jest.fn(() => ({}));
export const mockPatch = jest.fn(() => ({}));

export default {
    get: mockGet,
    post: mockPost,
    patch: mockPatch,
};
