export const mockSign = jest.fn(() => {});
export const mockVerify = jest.fn(() => {});
export const mockDecode = jest.fn(() => {});

export default {
    sign: mockSign,
    verify: mockVerify,
    decode: mockDecode,
};
